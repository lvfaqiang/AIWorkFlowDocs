# 模块知识库文档模板

> 适用于：模块开发工作流 ⑥ 知识库更新。
> 用途：模块实现完成后，沉淀关键信息供后续维护和跨会话恢复参考。
> 存放位置：`chapters/{NN}-{模块名}/knowledge.md`
> 创建时机：模块 QA 通过 + 验证对比通过后。

---

```markdown
# {模块名} — 知识库

> 完成日期：{YYYY-MM-DD}
> 实现方案：[impl-plan.md](impl-plan.md)
> UI 文档：[ui-plan.md](ui-plan.md)

## 一、模块概述

（一句话描述本模块的职责和功能范围）

## 二、文件清单

### 新建的文件

| 文件路径 | 职责 |
|---------|------|

### 修改的文件

| 文件路径 | 修改内容 |
|---------|---------|

## 三、关键实现决策

（记录实现过程中做出的重要技术决策，以及决策理由）

| 决策点 | 选择 | 理由 | 备选方案 |
|--------|------|------|---------|

## 四、与方案的偏差

（记录实际实现与 impl-plan.md 的差异，以及偏差原因）

| 变更类型 | 偏差点 | 方案描述 | 实际实现 | 原因 |
|----------|--------|---------|---------|------|

> 变更类型：新增 / 修改 / 删除（与 impl-plan.md §8 需求变更记录联动）

如无偏差，标注"无偏差，完全按方案实现"。

## 五、新增的设计 Token

（本模块实现过程中新增到设计系统的颜色/字体/间距）

| Token 名 | 值 | 用途 |
|----------|-----|------|

如无新增，标注"无新增"。

## 六、提取的通用组件

（本模块实现过程中提取到公共组件库的组件）

| 组件名 | 文件路径 | 用途 | 使用页面 |
|--------|---------|------|---------|

如无提取，标注"无提取"。

## 七、已知问题与技术债

（本模块遗留的已知问题、临时方案、待优化项）

| 问题 | 影响 | 优先级 | 建议处理时机 |
|------|------|--------|------------|

如无问题，标注"无已知问题"。

## 八、预先存在的问题

（实现过程中发现的、非本模块引入的问题）

| 问题 | 所属模块 | 影响 |
|------|---------|------|

如无发现，标注"无"。

## 九、依赖关系

### 本模块依赖

（列出本模块依赖的其他模块/公共能力）

### 依赖本模块的

（列出哪些后续模块会依赖本模块的产出）

## 十、维护指南

（给后续维护者的关键提示，如：修改某个逻辑时需要同步修改哪里、容易踩的坑等）
```
