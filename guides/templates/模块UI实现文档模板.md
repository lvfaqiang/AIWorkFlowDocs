# 模块 UI 实现文档模板

> 适用于：项目启动工作流 §4（模块实现前的 UI 文档整理）。
> 用途：在编写模块实现方案之前，先整理该模块所有页面的 UI 细节。
> 存放位置：`chapters/{NN}-{模块名}/ui-plan.md`

---

```markdown
# {模块名} — UI 实现文档

> 信息来源：{源项目布局文件 / Figma 设计稿链接}
> 关联页面：{列出本模块包含的所有页面}

## 一、页面清单

| 页面名 | 对应源文件/设计稿节点 | 入口方式 | 说明 |
|--------|---------------------|---------|------|

## 二、页面详情

### {页面名}

#### 2.1 布局结构

（描述页面的层级结构，从外到内）

```
页面根容器
├── 顶部导航栏
│   ├── 返回按钮
│   └── 标题文本
├── 内容区域（ScrollView / List）
│   ├── {区块1}
│   │   ├── ...
│   │   └── ...
│   └── {区块2}
└── 底部操作栏（如有）
```

#### 2.2 组件清单

| 组件 | 类型 | 位置 | 说明 |
|------|------|------|------|

类型标记：标准组件 / 自定义组件 / 通用组件（已在公共组件库中）

#### 2.3 样式细节

**容器/背景**：

| 元素 | 背景色 | 圆角 | 边框 | 阴影 | 间距 |
|------|--------|------|------|------|------|

**文本**：

| 元素 | 字体大小 | 字重 | 颜色 | 行数限制 | 对齐 |
|------|---------|------|------|---------|------|

**图片/图标**：

| 元素 | 尺寸 | 圆角 | 占位图 | 错误图 | 内容模式 |
|------|------|------|--------|--------|---------|

#### 2.4 状态变化

| 条件 | 影响元素 | 变化内容 |
|------|---------|---------|

示例：
- 用户未登录 → 头像显示默认图，昵称显示"未登录"
- 列表为空 → 隐藏列表，显示空状态视图
- VIP 用户 → VIP 标识可见，价格标签隐藏
- 按钮禁用 → 背景色变为 #CCCCCC，不可点击

#### 2.5 交互行为

| 触发元素 | 事件类型 | 行为 |
|---------|---------|------|

事件类型：点击 / 长按 / 滑动 / 下拉刷新 / 上拉加载 / 手势

#### 2.6 数据绑定

| UI 元素 | 数据字段 | 格式化规则 |
|---------|---------|-----------|

#### 2.7 空状态与错误状态

| 状态 | 展示内容 | 操作 |
|------|---------|------|

状态：加载中 / 空数据 / 网络错误 / 服务器错误 / 无权限

---

（对本模块的每个页面重复上述结构）

## 三、新增设计 Token

（实现本模块时需要新增到设计系统的颜色/字体/间距，如果都已存在则标注"无新增"）

| Token 名 | 值 | 用途 |
|----------|-----|------|

## 四、通用组件候选

（本模块中可能需要提取为通用组件的 UI 模式，标注复用场景）

| 组件 | 当前页面 | 潜在复用页面 | 是否提取 |
|------|---------|-------------|---------|

决策规则：当前模块内先实现，确认复用需求后再提取。
```
