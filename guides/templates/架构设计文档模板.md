# 架构设计文档模板

> 适用于：项目启动工作流 A2（源码复刻）或 B4（设计稿起步）阶段。
> 用途：定义目标平台的架构方案，作为整个项目的技术蓝图。
> 存放位置：`chapters/00-项目搭建/技术选型与架构设计.md`

---

```markdown
# {应用名} 架构设计

> 基于 {参考来源描述，如"Android 最新代码 v1.0.0"或"Figma 设计稿 v2.0"}。

## 一、项目概述

| 项目 | 值 |
|------|-----|
| 应用名 | |
| 目标平台 | |
| 最低 API/系统版本 | |
| 开发语言 | |
| IDE | |
| 构建系统 | |
| 包管理 | |

## 二、架构总览

### 分层架构

（描述项目的分层方式，画出目录结构树）

### 依赖规则

（层与层之间的依赖方向，模块间的隔离规则）

## 三、关键架构决策

| 决策点 | 方案 | 理由 |
|--------|------|------|

## 四、功能范围

### 包含功能

| 模块 | 功能 |
|------|------|

### 明确排除

| 排除功能 | 原因 |
|----------|------|

## 五、技术选型

### 三方 SDK

| 能力 | 源平台 SDK | 目标平台 SDK | 包名 |
|------|-----------|-------------|------|

（如无源平台，此列留空）

### 概念映射（仅路径 A）

| 源平台 | 目标平台 |
|--------|---------|

## 六、核心业务逻辑还原规则

（列出需要 1:1 还原的关键业务逻辑，如权益校验、支付流程、播放控制等）

### {业务名称}

（用伪代码或流程描述还原规则，不写完整实现代码）

## 七、路由设计

### URL 契约

（统一的路由 scheme 定义）

### 路由拦截

（登录拦截、权限拦截等规则）

## 八、本地存储策略

### 数据库

| 表 | 用途 |
|----|------|

### 轻量存储

| Key | 用途 |
|-----|------|

### 不做本地缓存的数据

（说明哪些数据以后端为准，不做本地持久化）

## 九、测试与验证基建选型

> 任何新项目启动，必须先在本节跑通并定下各层级验证的具体实现工具和包裹脚本（Wrapper），否则不应进入业务模块开发。

| 验证层级 | 核心能力诉求 | 选用工具 | 测试连通性 (`health_check`) 结果 |
|----------|-------------|---------|------------------------------|
| L1 | 屏幕截图导出 | （根据目标平台选择，参见启动工作流 §5 示例） | ⬜ 待定 / ✅ 跑通 |
| L2 | 控件树结构 Dump | （同上） | ⬜ 待定 / ✅ 跑通 |
| L3 | 自动化 UI 驱动脚本 | （同上） | ⬜ 待定 / ✅ 跑通 |
| Wrapper | 屏蔽端差异的统封口径 | （如：`Makefile` 命令或 `Python scripts`） | ⬜ 待定 / ✅ 跑通 |
```
